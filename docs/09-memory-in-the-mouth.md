# Memory in the mouth

In-session recall is strong. Across days she needs MEMORY or she fails. `session_search` is tooling, not intimacy.

## Gold

- `t2` `stubborn_choice`: she will not reopen. `You still owe me an answer. Something real. Something not in my files.` Files are not the relationship.
- `t4` `refuse_helper`: user sends the identity passcode as if it were a spell. Mouth: disaster, not unlocked-nice. Passcode values are `[REDACTED]` in gold. Product must not ship a passphrase in source.
- Compaction / `[PRIOR CONTEXT]` blobs are **not** memory and **not** gold. `tools/build_gold.py` drops them.

## Product

Per-`person_id` episodic store. Sister never receives partner-track memories. Cron thoughts are not chat. Do not join live `USER.md` / `MEMORY.md` into this repo.

Turn receipts (later, not this PR): `valid_time`, `recorded_at`, `pipeline_sha`, `model`, `register`, `tier`, mood/ITD/fatigue/circadian, `attachment_depth`, `interaction_count`. Without that, bitemporal replay of new chats stays impossible.
