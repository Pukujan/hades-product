# Handoff

## Done

Engine map: `docs/11-research-engine-map.md`. Three copies documented. WSL live self is `emotion-project-host-v2`; git is July code; memories is Sep export.

## Next

Port `hades_emotion.core` + circadian/fatigue tick into `hades_runtime/` using **native scales** (not 0–100). Do not copy live yaml/jsonl. Do not commit `.env`.

## Fail if

- Live `relations.yaml` / thoughts copied into this repo
- Qwen as architecture
